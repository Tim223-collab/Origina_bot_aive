# 🐧 Деплой на Ubuntu Сервер - Сводка

## ✅ Что Добавлено

### 1. Автоматическая Установка

**Файл:** `install_ubuntu.sh`

Один скрипт который:
- ✅ Обновляет систему
- ✅ Устанавливает Python 3.9+
- ✅ Создает venv
- ✅ Устанавливает все зависимости
- ✅ Устанавливает Playwright + браузеры
- ✅ Создает директории
- ✅ Создает .env файл
- ✅ Создает systemd service
- ✅ Создает вспомогательные скрипты

**Использование:**
```bash
chmod +x install_ubuntu.sh
./install_ubuntu.sh
```

---

### 2. Systemd Service

**Файл:** `/etc/systemd/system/aive-bot.service`

Возможности:
- ✅ Автозапуск при перезагрузке сервера
- ✅ Автоматический перезапуск при падении
- ✅ Логирование в файлы
- ✅ Управление через systemctl
- ✅ Изоляция пользователя

**Управление:**
```bash
sudo systemctl start aive-bot      # Запуск
sudo systemctl stop aive-bot       # Остановка
sudo systemctl restart aive-bot    # Перезапуск
sudo systemctl status aive-bot     # Статус
sudo systemctl enable aive-bot     # Автозапуск
sudo systemctl disable aive-bot    # Отключить автозапуск
```

---

### 3. Вспомогательные Скрипты

#### `update.sh`
Обновление бота:
```bash
./update.sh
```

Делает:
- Останавливает бота
- Обновляет код (git pull)
- Обновляет зависимости
- Перезапускает бота
- Показывает статус

#### `monitor.sh`
Мониторинг бота:
```bash
./monitor.sh
```

Показывает:
- Статус (running/stopped)
- PID процесса
- Uptime (как давно запущен)
- Использование памяти
- Использование CPU
- Размер БД
- Последние логи

#### `backup.sh`
Бэкап базы данных:
```bash
./backup.sh
```

Делает:
- Копирует bot.db
- Сжимает в .gz
- Сохраняет в ~/backups/aive-bot/
- Удаляет старые бэкапы (>7 дней)

**Автоматический бэкап:**
```bash
crontab -e
# Добавь: 0 3 * * * ~/projects/aive-bot/backup.sh
```

---

### 4. Документация

#### `docs/UBUNTU_DEPLOY.md` (Полная Инструкция)

**Содержание:**
1. Требования
2. Подготовка сервера
3. Клонирование проекта
4. Установка зависимостей
5. Настройка конфигурации
6. Тестовый запуск
7. Запуск в фоне (Systemd)
8. Просмотр логов
9. Автоматическое обновление
10. Мониторинг
11. Резервное копирование
12. Решение проблем
13. Безопасность

**Объем:** 600+ строк, 50+ команд

#### `UBUNTU_QUICKSTART.md` (Быстрый Старт)

**Содержание:**
- Автоматическая установка (5 команд)
- Основные команды
- Проверка работы
- Решение проблем

**Объем:** Краткая выжимка для быстрого старта

---

## 🎯 Преимущества Деплоя

### Надежность
✅ **Автоперезапуск** - бот сам перезапускается при падении
✅ **Автозапуск** - запускается при перезагрузке сервера
✅ **Логирование** - все логи сохраняются
✅ **Мониторинг** - скрипт для проверки статуса

### Удобство
✅ **Один скрипт** - автоматическая установка
✅ **Systemd** - стандартное управление
✅ **Вспомогательные скрипты** - update, monitor, backup
✅ **Документация** - подробные инструкции

### Производительность
✅ **Фоновый режим** - не занимает терминал
✅ **Оптимизация** - venv изоляция
✅ **Ресурсы** - ~100-200MB RAM
✅ **Стабильность** - работает 24/7

---

## 📊 Структура Файлов

```
Origina_bot_aive/
├── install_ubuntu.sh          # Скрипт установки ✨
├── update.sh                  # Обновление ✨
├── monitor.sh                 # Мониторинг ✨
├── backup.sh                  # Бэкап ✨
├── .env                       # Конфигурация
├── main.py                    # Точка входа
├── requirements.txt           # Зависимости
│
├── docs/
│   ├── UBUNTU_DEPLOY.md      # Полная инструкция ✨
│   └── ...
│
├── UBUNTU_QUICKSTART.md       # Быстрый старт ✨
├── DEPLOY_SUMMARY.md          # Эта сводка ✨
│
├── data/                      # База данных
│   ├── bot.db
│   └── screenshots/
│
├── logs/                      # Логи
│   ├── bot.log
│   └── error.log
│
└── venv/                      # Виртуальное окружение
```

**✨ = Новые файлы**

---

## 🚀 Пошаговая Установка

### 1. Загрузка на Сервер

**Вариант A: Git**
```bash
cd ~
git clone <your-repo> aive-bot
cd aive-bot
```

**Вариант B: SCP**
```bash
# С локальной машины
scp -r /path/to/Origina_bot_aive user@server:~/aive-bot
```

**Вариант C: SFTP/FileZilla**
- Загрузи через GUI

### 2. Запуск Установки

```bash
cd ~/aive-bot
chmod +x install_ubuntu.sh
./install_ubuntu.sh
```

**Время:** ~5-10 минут

### 3. Настройка

```bash
nano .env
```

Заполни API ключи:
```env
TELEGRAM_BOT_TOKEN=твой_токен
ALLOWED_USER_IDS=твой_id
DEEPSEEK_API_KEY=ключ
GEMINI_API_KEY=ключ
OPENAI_API_KEY=ключ
```

### 4. Запуск

```bash
sudo systemctl start aive-bot
sudo systemctl status aive-bot
```

### 5. Проверка

```bash
# Логи
sudo journalctl -u aive-bot -f

# Мониторинг
./monitor.sh

# Telegram
# Напиши боту: /start
```

---

## 🎓 Полезные Команды

### Управление Ботом

```bash
# Запуск
sudo systemctl start aive-bot

# Остановка
sudo systemctl stop aive-bot

# Перезапуск
sudo systemctl restart aive-bot

# Статус
sudo systemctl status aive-bot

# Включить автозапуск
sudo systemctl enable aive-bot

# Отключить автозапуск
sudo systemctl disable aive-bot
```

### Логи

```bash
# Реальное время
sudo journalctl -u aive-bot -f

# Последние 100 строк
sudo journalctl -u aive-bot -n 100

# Только ошибки
sudo journalctl -u aive-bot -p err

# За сегодня
sudo journalctl -u aive-bot --since today

# Файловые логи
tail -f logs/bot.log
tail -f logs/error.log
```

### Мониторинг

```bash
# Скрипт мониторинга
./monitor.sh

# Использование ресурсов
top -p $(pgrep -f "python3 main.py")

# Размер БД
du -h data/bot.db

# Использование диска
du -sh ~/aive-bot/*
```

### Обслуживание

```bash
# Обновление
./update.sh

# Бэкап
./backup.sh

# Просмотр бэкапов
ls -lh ~/backups/aive-bot/

# Восстановление из бэкапа
gunzip -c ~/backups/aive-bot/bot_backup_YYYYMMDD.db.gz > data/bot.db
sudo systemctl restart aive-bot
```

---

## ⚠️ Решение Проблем

### Бот не запускается

```bash
# Проверь логи
sudo journalctl -u aive-bot -n 50

# Проверь .env
cat .env | grep "your_"
# Если что-то нашлось - не все заполнено!

# Проверь права
ls -la .env
# Должно быть: -rw------- (600)

# Тестовый запуск
source venv/bin/activate
python3 main.py
```

### Ошибки Playwright

```bash
source venv/bin/activate
playwright install chromium
sudo playwright install-deps
```

### Нехватка памяти

```bash
# Проверить
free -h

# Добавить swap
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

### Бот падает

```bash
# Проверь ошибки
tail -n 100 logs/error.log

# Перезапусти
sudo systemctl restart aive-bot

# Если не помогает - запусти вручную для отладки
source venv/bin/activate
python3 main.py
```

---

## 📈 Оптимизация

### Ограничение Памяти (Systemd)

Отредактируй service:
```bash
sudo nano /etc/systemd/system/aive-bot.service
```

Добавь в `[Service]`:
```ini
MemoryMax=500M
MemoryHigh=400M
```

Перезагрузи:
```bash
sudo systemctl daemon-reload
sudo systemctl restart aive-bot
```

### Ротация Логов

```bash
sudo nano /etc/logrotate.d/aive-bot
```

Содержимое:
```
/home/YOUR_USERNAME/aive-bot/logs/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
}
```

---

## 🔒 Безопасность

### Защита .env

```bash
chmod 600 .env
```

### Firewall (UFW)

```bash
sudo apt install ufw
sudo ufw allow ssh
sudo ufw enable
```

### Автообновления

```bash
sudo apt install unattended-upgrades
sudo dpkg-reconfigure -plow unattended-upgrades
```

---

## 📊 Статистика

### Созданные Файлы
- ✅ `install_ubuntu.sh` (300+ строк)
- ✅ `update.sh`
- ✅ `monitor.sh`
- ✅ `backup.sh`
- ✅ `docs/UBUNTU_DEPLOY.md` (600+ строк)
- ✅ `UBUNTU_QUICKSTART.md`
- ✅ `DEPLOY_SUMMARY.md` (этот файл)

### Функциональность
- ✅ Автоматическая установка
- ✅ Systemd service
- ✅ Автоперезапуск
- ✅ Логирование
- ✅ Мониторинг
- ✅ Бэкапы
- ✅ Обновления

---

## 🎉 Итог

**AIVE v2.5.2** полностью готов для продакшн деплоя на Ubuntu!

**Одной командой:**
```bash
./install_ubuntu.sh
```

**Запускается бот который работает 24/7 с:**
- ✅ Автоперезапуском
- ✅ Логированием
- ✅ Мониторингом
- ✅ Бэкапами

**Документация:**
- `docs/UBUNTU_DEPLOY.md` - полная инструкция
- `UBUNTU_QUICKSTART.md` - быстрый старт

---

**Made with 💙 by AIVE Team**
**Version 2.5.2 - 21.11.2024**

