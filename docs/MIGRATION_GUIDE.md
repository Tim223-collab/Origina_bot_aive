# 🔄 Migration Guide v1.x → v2.0

Это руководство по обновлению с версии 1.x на версию 2.0.

---

## 📊 Что изменилось

### Добавлено
- ✅ Gemini 2.0 Flash (бесплатный!)
- ✅ Гибридная AI система
- ✅ Memory Bank
- ✅ Улучшенная структура проекта

### Удалено
- ❌ 41 мусорный файл
- ❌ Устаревшая документация
- ❌ Дубликаты

### Изменено
- 🔄 requirements.txt (новые зависимости)
- 🔄 config.py (новые переменные)
- 🔄 Структура сервисов

---

## ⚡ Быстрая миграция (5 минут)

### Шаг 1: Backup
```bash
# Сделай backup базы данных
cp data/bot.db data/bot_backup.db

# Сделай backup .env
cp .env .env.backup
```

### Шаг 2: Pull изменений
```bash
git pull origin main
```

### Шаг 3: Обновить зависимости
```bash
pip install -r requirements.txt
```

### Шаг 4: Добавить Gemini API ключ
Добавь в `.env`:
```env
# Google Gemini API (бесплатно!)
GEMINI_API_KEY=your_key_here
```

**Как получить:**
1. Перейди на https://aistudio.google.com/app/apikey
2. Войди через Google аккаунт
3. Нажми "Create API Key"
4. Скопируй ключ
5. Вставь в `.env`

### Шаг 5: Перезапустить бота
```bash
python main.py
```

**Готово!** 🎉

---

## 🔍 Детальная миграция

### 1. Обновление зависимостей

**Новые пакеты:**
- `google-generativeai>=0.8.0` - Gemini API
- `anthropic>=0.18.0` - Claude API (опционально)
- `chromadb>=0.4.0` - Векторная БД (для будущих фич)
- `sentence-transformers>=2.2.0` - Embeddings (для будущих фич)

**Установка:**
```bash
pip install -r requirements.txt
```

### 2. Обновление .env

**Обязательные (как было):**
```env
TELEGRAM_BOT_TOKEN=your_token
ALLOWED_USER_IDS=your_id
DEEPSEEK_API_KEY=sk-xxx
```

**Новые (рекомендуемые):**
```env
# Gemini (бесплатно!)
GEMINI_API_KEY=AIzaSyXXXXXXXXXXXXXXX

# Claude (опционально, для эмоциональной поддержки)
ANTHROPIC_API_KEY=sk-ant-xxx
```

**Как работает теперь:**
- Если есть только DEEPSEEK - работает как раньше
- Если есть GEMINI - основные диалоги бесплатные!
- Если есть оба - умный выбор модели
- Если есть еще и CLAUDE - максимальное качество

### 3. Изменения в config.py

**Было:**
```python
if not DEEPSEEK_API_KEY:
    errors.append("DEEPSEEK_API_KEY не установлен")
```

**Стало:**
```python
if not DEEPSEEK_API_KEY and not GEMINI_API_KEY:
    errors.append("Нужен хотя бы один AI API ключ")
```

**Что это значит:**
- Можно использовать только Gemini (бесплатно)
- Можно использовать только DeepSeek (платно)
- Можно использовать оба (рекомендуется)

### 4. Новые сервисы

**services/gemini_service.py** - Gemini API клиент
**services/hybrid_ai_service.py** - Умный выбор модели

**Старый код продолжает работать!** Обратная совместимость сохранена.

### 5. Memory Bank

**Новая структура:**
```
memory-bank/
├── projectBrief.md       # Цели проекта
├── productContext.md     # Product vision
├── systemPatterns.md     # Архитектура
├── techContext.md        # Технический стек
├── activeContext.md      # Текущий контекст
└── progress.md           # Прогресс
```

**Для чего:**
- Документация проекта
- Понимание архитектуры
- Tracking прогресса

**Нужно ли что-то делать:** Нет, это автоматически создано.

---

## 💰 Экономия на API

### До обновления
- DeepSeek: ~$5/мес

### После обновления
- Gemini (бесплатно): $0
- DeepSeek (только reasoning): ~$0.50/мес
- **Итого**: ~$0.50/мес

**Экономия: $4.50/мес (90%)**

---

## 🧪 Проверка после миграции

### 1. Проверь config
```bash
python test_config.py
```

**Ожидаемый вывод:**
```
✅ Конфигурация валидна!
```

Или (если нет Gemini):
```
⚠️ GEMINI_API_KEY не установлен - рекомендуется (бесплатно!)
✅ Конфигурация валидна!
```

### 2. Запусти бота
```bash
python main.py
```

**Ожидаемый вывод:**
```
🚀 Инициализация бота...
✅ База данных инициализирована
✅ Обработчики зарегистрированы
✅ Job queue настроена
✅ Бот готов к запуску!
```

### 3. Проверь в Telegram

Напиши боту:
```
Привет!
```

Бот должен ответить нормально (используя Gemini если ключ добавлен).

### 4. Проверь reasoning mode
```
/think Почему небо голубое?
```

Должен использовать DeepSeek Reasoner.

---

## ❓ FAQ

### Q: Что если я не хочу добавлять Gemini?
**A:** Всё будет работать как раньше через DeepSeek. Но рекомендуется добавить - это бесплатно и лучше!

### Q: Нужно ли удалять старые файлы вручную?
**A:** Нет, уже удалено автоматически при pull.

### Q: Сломается ли что-то?
**A:** Нет, обратная совместимость сохранена на 100%.

### Q: Можно ли откатиться назад?
**A:** Да:
```bash
git checkout <old-commit>
pip install -r requirements.txt
cp .env.backup .env
python main.py
```

### Q: Где лимиты Gemini?
**A:** 1500 запросов/день бесплатно. Для personal use более чем достаточно.

### Q: Что если превышу лимит Gemini?
**A:** Автоматический fallback на DeepSeek (платно но работает).

### Q: Как узнать какая модель используется?
**A:** Добавим команду `/ai_stats` в следующем обновлении. Пока смотри логи.

### Q: Можно ли выбирать модель вручную?
**A:** Пока нет, выбор автоматический. Но можем добавить если нужно.

---

## 🐛 Известные проблемы

### Проблема: ModuleNotFoundError: No module named 'google.generativeai'
**Решение:**
```bash
pip install --upgrade google-generativeai
```

### Проблема: Gemini API error 400
**Решение:** Проверь правильность API ключа в `.env`

### Проблема: Бот не отвечает
**Решение:**
1. Проверь логи
2. Убедись что хотя бы один AI API ключ валидный
3. Перезапусти бота

---

## 📞 Поддержка

**Проблемы после миграции?**

1. Проверь backup: `ls -la data/bot_backup.db`
2. Проверь .env: `cat .env | grep API_KEY`
3. Проверь зависимости: `pip list | grep -E "google-generativeai|anthropic"`
4. Посмотри логи бота

**Критическая проблема?**

Откат:
```bash
git checkout <previous-version>
cp .env.backup .env
cp data/bot_backup.db data/bot.db
pip install -r requirements.txt
python main.py
```

---

## ✅ Checklist миграции

- [ ] Backup базы данных
- [ ] Backup .env
- [ ] Pull изменений
- [ ] Обновить requirements.txt
- [ ] Добавить GEMINI_API_KEY
- [ ] Запустить test_config.py
- [ ] Запустить бота
- [ ] Проверить в Telegram
- [ ] Удалить backups (через неделю)

---

**Обновлено**: 21.11.2024
**Версия**: 2.0.0

