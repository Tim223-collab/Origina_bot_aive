# ⚡ Ubuntu - Быстрый Старт

## 🚀 Автоматическая Установка (5 минут)

```bash
# 1. Загрузи проект на сервер
cd ~
git clone <your-repo> aive-bot
cd aive-bot

# 2. Запусти скрипт установки
chmod +x install_ubuntu.sh
./install_ubuntu.sh
```

Скрипт автоматически:
- ✅ Установит все зависимости
- ✅ Создаст виртуальное окружение
- ✅ Установит Playwright
- ✅ Создаст systemd service
- ✅ Создаст вспомогательные скрипты

```bash
# 3. Отредактируй .env (ОБЯЗАТЕЛЬНО!)
nano .env
```

Заполни:
```env
TELEGRAM_BOT_TOKEN=твой_токен
ALLOWED_USER_IDS=твой_telegram_id
DEEPSEEK_API_KEY=твой_ключ
GEMINI_API_KEY=твой_ключ
OPENAI_API_KEY=твой_ключ
```

Сохрани: `Ctrl+O`, Enter, `Ctrl+X`

```bash
# 4. Запусти бота
sudo systemctl start aive-bot

# 5. Проверь статус
sudo systemctl status aive-bot
```

Должно быть: **active (running)** ✅

```bash
# 6. Проверь логи
sudo journalctl -u aive-bot -f
```

Нажми `Ctrl+C` чтобы выйти из логов.

---

## 🎯 Основные Команды

```bash
# Управление
sudo systemctl start aive-bot      # Запустить
sudo systemctl stop aive-bot       # Остановить  
sudo systemctl restart aive-bot    # Перезапустить
sudo systemctl status aive-bot     # Статус

# Логи
sudo journalctl -u aive-bot -f     # В реальном времени
sudo journalctl -u aive-bot -n 100 # Последние 100 строк

# Вспомогательные скрипты
./monitor.sh    # Мониторинг (CPU, память, uptime)
./update.sh     # Обновление бота
./backup.sh     # Бэкап базы данных
```

---

## ✅ Проверка Работы

1. **Бот запущен:**
```bash
sudo systemctl status aive-bot
# Должно быть: active (running)
```

2. **Процесс работает:**
```bash
ps aux | grep "python3 main.py"
```

3. **Нет ошибок в логах:**
```bash
tail -n 20 logs/bot.log
```

4. **Telegram бот отвечает:**
- Напиши боту: `/start`

---

## ⚠️ Если Не Работает

```bash
# Проверь логи
sudo journalctl -u aive-bot -n 50

# Проверь .env
cat .env

# Проверь что все ключи заполнены
grep "your_" .env

# Если есть "your_" в выводе - не все заполнено!

# Перезапусти
sudo systemctl restart aive-bot
```

---

## 📚 Полная Документация

- **Детальная инструкция:** [docs/UBUNTU_DEPLOY.md](docs/UBUNTU_DEPLOY.md)
- **Решение проблем:** раздел "Решение Проблем" в UBUNTU_DEPLOY.md

---

## 🎉 Готово!

Бот работает 24/7 на твоем сервере! 🚀

**Telegram:** Напиши боту `/start`
**Мониторинг:** `./monitor.sh`

